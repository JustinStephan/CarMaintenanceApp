// src/styles/DateInputStyles.js
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    dateButton: {
        fontSize: 16,
        color: '#6200ee',
        paddingVertical: 15,
        textAlign: 'center',
        borderWidth: 1,
        borderColor: '#6200ee',
        borderRadius: 8,
        marginBottom: 20,
    },
    calendar: {
        marginTop: 20,
        borderRadius: 8,
    },
});

export default styles;
